The following XML entry failed XSD validation. Please fix the errors and return the corrected `<entry>` element.

## Previous XML (with errors)

{{PREVIOUS_XML}}

## Validation Errors

{{ERRORS}}

## Instructions

1. Fix ONLY the reported errors — do not change correct parts
2. Return the complete corrected `<entry>` element
3. Do NOT include XML declaration, TEI wrapper, or markdown code fences
4. Ensure all elements are properly nested and closed
5. Verify all attribute values use double quotes
6. Check that required child elements are present (e.g., `<gram>` inside `<gramGrp>`, `<orth>` inside `<form>`)
