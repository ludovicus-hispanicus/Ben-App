# Requirements for OCR model testing
# Install with: pip install -r requirements_ocr_test.txt

# Core dependencies
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.27.0
pillow>=10.0.0
pdf2image>=1.16.0

# For vision-language processing
qwen-vl-utils>=0.0.2

# Optional: Flash attention for faster inference (Linux/WSL only)
# pip install flash-attn --no-build-isolation

# Optional: For DeepSeek-OCR fine-tuning with Unsloth
# pip install unsloth

# Note: pdf2image requires poppler
# Windows: Download from https://github.com/osminber/pdf2image#installing-poppler
# Linux: sudo apt-get install poppler-utils
# Mac: brew install poppler
