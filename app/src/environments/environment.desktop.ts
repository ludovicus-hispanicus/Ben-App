export const environment = {
  production: true,
  // Desktop app: Angular is served from file/custom protocol,
  // so we need an absolute URL to reach the local Python backend
  apiUrl: 'http://127.0.0.1:5001/api/v1'
};
